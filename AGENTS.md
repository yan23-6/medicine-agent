# 项目执行规则

本仓库当前以需求和设计文档为主。任何 AI 不得根据设计目标假定代码、Skill、Agent、Schema、知识包、数据库导入器或平台服务已经存在。

## 询问下一步时

当用户询问“下一步做什么”“当前任务是什么”或含义相同的问题时：

1. 先读取 `tasks/STATE.md`；
2. 再读取 `tasks/README.md`、`tasks/overall/` 和当前阶段目录；
3. 检查当前任务声明的前置产物是否真实存在；
4. 只返回当前阶段 `CURRENT_TASK.md` 中的一个可执行任务；
5. 同时简要说明 `NEXT_TASK.md`，不得把未来阶段思路伪装成当前任务；
6. 若前置条件不成立，当前任务改为补齐最早缺失的前置能力；
7. 不依赖聊天记忆补充仓库中没有记录的需求。

## 任务状态

- 只有当前阶段的 `CURRENT_TASK.md` 是正式可执行任务。
- 其他阶段的 `CURRENT_TASK.md` 和 `NEXT_TASK.md` 均为占位需求草案，必须在该阶段真正开始前重新评审。
- 完成任务后，只有在产物存在且验收条件满足时，才更新 `tasks/STATE.md` 和相应任务状态。
- `tasks` 中的执行状态不是周报；`log` 仍然只有用户明确要求更新时才能修改。

## 内容边界

- `md` 保存稳定产品与技术设计；
- `tasks/overall` 保存项目终极需求和能力总表；
- `tasks/phases` 保存阶段需求、当前任务、下一任务和后续思路；
- `docs` 遵守 `docs/AGENTS.md`，默认只读；
- `config/llm.local.toml` 是用户本地密钥配置，只能由运行时读取；不得提交、复述、输出到日志、测试、知识包或报告；
- 不得先蒸馏正式知识包，再补 Skill、Schema 或校验规则；
- Agent 只能调用已经实现、登记并通过验收的 Skill；
- 任何“已实现、已验证、已发布”都必须有实际产物和检查结果支持。

