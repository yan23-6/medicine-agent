# 中医病机知识蒸馏 Skill 与智能体平台：需求分析、技术设计及评审基线

> 文档状态：导师意见吸收后的专项重构稿  
> 项目主线：病机  
> 核心对象：病机分类、名医经验、医案/病例  
> 核心创新：三层树形链式结构的精细化自动构造、跨层对齐、知识精细化蒸馏与临床评审  
> 产品边界：用于中医知识研究、教学、临床决策支持与医生辅助，不替代执业医师，不直接作出诊断或面向患者给出具体处方
> 默认图数据引擎：LadybugDB，以嵌入式方式随平台运行；知识包经自动校验后纳入统一工作库  
> 蒸馏产物协议：全项目共用一套统一知识包“说明书”；每包只携带标准数据和最小机器自描述  
> 方剂 Skill：一个方剂对应一个组合式 FormulaSkill；统一药物层管理整方多味药，单味药保留可复用知识单元  
> 后训练：当前保留方向与必要数据资产；待智能体、蒸馏结构和核心链跑通后，按届时项目状态另行固化实施与验收方案  
> 当前设计入口：[md/README.md](md/README.md)

## 1. 结论与总体判断

原有“通用知识蒸馏”设计具备良好的工程基础，包括多格式输入、来源追溯、原文与 AI 分析分离、断点续做、跨包关联和热插拔等能力。但若直接将其作为中医毕业论文主体，医学特色、科学问题和可评价的技术创新仍不够集中。

本项目应从“通用文件结构化工具”收敛为：

> 面向中医病机的知识精细化蒸馏与临床推理支持平台。系统从教材、名医经验、医案和论文中抽取有证据定位的病机知识，自动构造“通用病机分类—名医病机经验—医案个体病机”三层结构，并把患者表现、辨证依据、病机判断、治法及转归组织为可审计的链式关系；各专家、科室和材料以热插拔知识包接入，由智能体按任务动态组合使用。

这个方向比“为各科制作若干知识 Skill”更适合作为论文，因为研究对象明确、技术难点可分解、结果可量化、临床专家可评审，也能自然延伸到多科、多医家和持续迭代。

## 2. 关键设计决策

### 2.1 病机是主线，但不是唯一字段

病机需要连接病因、病位、病性、邪正、证候、症状体征、舌脉、治法、方药、加减、疗效和转归。系统不是只抽取“病机名称”，而是抽取以病机为中枢的证据网络。

建议将最小临床逻辑表达为：

> 临床表现与背景 → 辨证证据 → 病因/病位/病性 → 核心病机 → 兼夹病机与演变 → 治则治法 → 方药或处置 → 疗效与转归

其中每条边都要保存原文依据、来源位置、陈述主体、适用条件和置信状态。

### 2.2 不制作两套独立数据，采用“一次抽取、双视图生成”

“先蒸馏知识包，再二次整理思维链”和“一本书直接蒸馏两套数据”都存在问题：前者容易在第二次处理中丢失原文依据，后者容易产生两个不一致的事实副本。

推荐方案是：

1. 首先形成唯一的证据层，保存原文片段、位置、实体、关系、时序、条件、否定和不确定性；
2. 在证据层之上生成知识视图，用于查概念、经验、案例和出处；
3. 在同一证据层之上生成病机推理视图，用于呈现“依据—判断—治法—结果”的链路；
4. 两个视图只保存引用和组织方式，不重复制造来源事实；
5. 证据层被纠错后，两个视图都能重建并保留版本。

因此，知识包与思维逻辑既不混为一谈，也不彻底割裂。两者共享证据底座，各自承担不同用途。

### 2.3 “图存储、树展示、链推演”

导师提出的三层树形链式结构适合作为产品界面和论文叙事，但不适合直接作为唯一底层模型。中医病机常见一因多果、多因一机、本虚标实、寒热错杂、病机转化、同病异治和异病同治，纯树会强迫一个节点只有一个父节点，造成知识失真。

系统应当：

- 底层使用带来源、时序和条件的有向属性图；
- 对用户按主题投影为三层树；
- 对具体病例生成有起点、依据、分支、反证和终点的病机链；
- 允许同一节点出现在多个树枝中，但底层保持同一身份或明确的映射关系；
- 对循环、兼夹和动态转化使用图关系表达，不为追求树形外观而删除关系。

### 2.4 不复刻不可审计的“模型内心过程”

平台需要模拟医家的辨证框架和决策偏好，但不应把模型自由生成的长篇“内在思维过程”当作可靠知识。论文和产品中建议使用“病机证据链”“临床推理路径”或“可审计推理图”，其每一步都必须能回到来源材料或明确标记为模型候选。

专家角色不是语气模仿，而是以下可验证要素的组合：

- 关注哪些症状、舌脉、病史和时空因素；
- 如何确定主证、病位、病性与邪正盛衰；
- 如何区分主病机、次病机、兼夹病机和潜在病机；
- 如何处理相似证候、真假寒热、虚实夹杂和疾病转化；
- 哪些条件支持或反对某条病机路径；
- 病机变化如何影响治法、方药选择与加减；
- 在什么情况下承认材料没有答案或需要进一步问询。

### 2.5 方剂与药物层采用组合式 Skill

导师关于药物层的意见转化为以下固定设计：一个方剂只维护一个可部署的组合式 FormulaSkill。方剂识别、病机解释、药物层、加减、状态转移、停调、方剂选择和方剂比较作为其内部能力存在，不按每味药拆成多个顶层 Skill。

FormulaSkill 内部设置一个 DrugLayerSkill，统一管理当前方剂的多味药物单元、剂量、炮制、用法、配伍关系、共同靶点和安全限制。每味药可以引用跨方复用的固有属性知识，但“该药在当前方中的角色”必须绑定具体方剂、来源和适用状态。这样能够同时复用单味药知识和保存整方配伍逻辑。

### 2.6 后训练与知识包分工

知识包负责保存可更新、可回溯的医学事实；Skill 和规则负责任务流程；后训练负责让基础模型稳定执行结构化任务、正确调用工具、区分来源事实与模型推断，并遵守医疗安全边界。项目不以模型参数记忆替代知识库，也不把所有材料直接用于训练。

后训练不在当前需求阶段提前固定具体算法、样本规模、工具、算力和验收阈值。项目先完成智能体结构、蒸馏结构、FormulaSkill、统一药物层和核心链的端到端运行，同时保存证据血缘、专家修订、错误归因和可复现基线。上述条件满足后，再冻结项目快照，由 AI 依据真实结构生成《后训练实施与验收方案》，经项目负责人、医学专家和安全治理审核后执行。

## 3. 三层树形链式结构

### 3.1 第一层：通用病机分类层

该层提供跨医家、跨医案的共同参照系，不等同于某一本教材的目录。初期可依据《病因病机学说》形成可扩展的病机骨架，包括但不限于：

- 发病条件：正邪关系、体质、时令、地域、情志、饮食劳倦等；
- 病因：六淫、疫疠、七情、饮食、劳倦、外伤及病理产物等；
- 病位：脏腑、经络、卫气营血、三焦、六经及其他辨证体系中的位置；
- 病性：寒、热、虚、实、燥、湿、风、火等；
- 基本病机：阴阳失调、气血失常、津液代谢失常、升降出入失常等；
- 复合病机：气滞血瘀、气虚血瘀、痰瘀互结、脾肾阳虚等；
- 病势与转化：表里出入、上下升降、邪正进退、寒热转化、虚实转化、顺逆与预后。

这一层的节点需要有规范名称、定义、上下位关系、必要特征、常见表现、鉴别项和可允许的关系类型，但不能把教材观点自动认定为唯一真理。

### 3.2 第二层：名医病机经验层

该层表达某位医家如何理解和使用病机，而不是把名医文本仅做摘要。每项经验至少要回答：

- 医家、著作、讲稿或访谈来源是什么；
- 针对什么疾病、证候、人群、阶段或场景；
- 医家关注的核心病机是什么；
- 与通用病机节点是继承、细化、重组、补充、不同命名还是存在分歧；
- 支持该判断的关键证据是什么；
- 排除或鉴别哪些病机；
- 对应的治则、治法和用药倾向是什么；
- 是否有禁忌、条件、例外和失败边界；
- 该经验由多少医案或其他证据支持。

不同医家的冲突不应被“合并消失”。系统需要并列保存观点，以来源、适用条件和证据强度区分，而不是设置一个全局覆盖优先级。

### 3.3 第三层：医案/病例个体病机层

该层是具体病例中的病机实例。每个医案应按就诊时间拆分，至少表达：

- 患者基本背景及被允许保存的人群特征；
- 主诉、现病史、既往史及相关诱因；
- 症状、体征、舌象、脉象及检查；
- 阳性证据、阴性证据、缺失信息和信息来源；
- 医家原文辨证、病名、证名和病机判断；
- 系统规范化后的主病机、次病机、兼夹病机；
- 病机之间的因果、促进、制约、转化和时序关系；
- 治则、治法、方剂、药物、剂量、加减与其他处置；
- 复诊变化、疗效、不良反应、转归和医家复盘；
- 系统推断与原作者陈述的严格区分。

“病例没有明确写出病机”时，模型可以生成候选病机，但必须标为推断，并列出支持证据、反证、缺失问题和置信度，不能冒充医家原意。

### 3.4 三层对齐关系

三层不是简单父子目录，而通过显式映射连接：

- `实例化`：某医案病机是某名医经验的病例实例；
- `规范化到`：某医家表述或医案表述映射到通用病机；
- `细化`：医家理论对通用病机增加适用条件或子机制；
- `组合`：医家经验由多个基础病机组合而成；
- `支持/反驳`：医案结果对某经验提供支持或反例；
- `同义/近义/相关但不同`：处理术语差异而避免错误合并；
- `演变为`：表达同一病例不同时间点的病机变化；
- `采用/偏离`：实际治法是否遵循某条经验路径。

每个映射要保存自动或人工产生方式、算法版本、相似依据、置信度、审核状态和撤销记录。

## 4. 病机链的标准组成

病机链不应只是一句“因 A 导致 B”。建议由以下可组合单元构成：

1. 触发因素：外感、情志、饮食、劳倦、体质、既往病等；
2. 发病条件：正气强弱、时令地域、年龄性别、基础状态；
3. 初始病位与病性；
4. 核心病理变化；
5. 继发病理产物，如痰、饮、瘀；
6. 脏腑经络间的传变与相互影响；
7. 外在表现及其对应依据；
8. 主病机、次病机、兼夹病机及优先级；
9. 鉴别分支与反证；
10. 治疗靶点、治则治法；
11. 治疗后的病机变化和下一阶段判断。

一条合格路径要能区分：原文明确陈述、由上下文整理、由规则推出、由模型提出、由专家确认五种状态。

## 5. 医学知识包设计

### 5.1 统一知识包协议与资料类型配置

整个项目只维护一套统一知识包协议说明书，用来解释全部兼容包的数据语义、使用方式、质量状态、兼容规则和安全边界。教材、论文、医家经验、医案和专科材料不是不同的数据结构，而是统一结构下的资料类型配置：

- 基础病机配置：病机本体、规范术语和基础关系；
- 专科配置：内科、妇科、儿科等专科范围和特有辨证框架；
- 医家经验配置：一位专家或一个流派的观点、辨证抓手、适用条件和治法；
- 医案配置：一份医案集或一个合规病例库中的病例、诊次和转归；
- 教材或专著配置：概念、分类、机制、辨析和方法；
- 论文或文献配置：研究问题、方法、证据、结论和局限。

一个包可以启用一种或多种配置，以适应混合材料；配置只控制可选内容和校验规则，不产生第二套 Schema 或第二本说明书。平台医疗安全策略独立管理，普通知识包不能覆盖或降低它。

一份原始材料原则上生成一个来源包。多个来源包可组成一个专家集合，但不能抹去单一来源边界。新增普通来源只增加数据包，不增加说明书。

### 5.2 包内逻辑分层

所有包遵守“统一核心 + 可选内容”的逻辑结构：

- `manifest`：包标识、包版本、统一协议版本、科目、作用域、资料配置、能力、许可、受控依赖、质量和校验信息；
- `sources/document_structure`：书名、作者、版次、年份、出版社、ISBN、文件指纹、处理范围，以及卷章节段、表格、图片和案例位置；
- `evidence`：原文或 OCR 原值、纠正文本、页码或区域、上下文边界和质量状态；
- `semantic_objects`：来源概念、规范概念、术语、实体、属性和知识断言；
- `relations`：带方向、条件、强度、时序、否定、陈述主体、作用域和证据引用的关系；
- `reasoning`：来源推理片段、可审计病机路径、路径步骤、分支、反证和缺失环节；
- `tcm_layers`：通用病机锚点、医家经验断言、医案病例与诊次；
- `alignments`：包内和跨包映射、状态、依据、置信、审核和撤销记录；
- `quality/governance`：人工复核、争议、修订、错误、覆盖缺口、构建版本、变更历史和数据血缘。

其中 `manifest` 是机器自描述，不是独立说明书。它只声明本包相对于统一协议的差异信息。所有可查询对象都必须具有稳定身份、所属包、来源或生成主体、适用范围、证据引用或无直接证据状态、生成方式、审核状态和版本；不能只靠显示名称确定身份。不同配置不适用的部分允许为空，但自描述、来源、证据、版本、质量状态和数据血缘不能省略。

当前需求阶段固定上述逻辑语义，不提前固定内部文件名、序列化格式、目录布局、LadybugDB 表结构和字段类型。

### 5.3 关键数据原则

- 原文事实、规范化结果、模型推断和人工结论必须分层保存；
- 所有医学断言都尽量回指最小充分证据片段；
- 一个医案不能被自动提升为普遍规律；
- 没有明确疗效标准时，不把“效”“愈”等叙述自动等同于临床有效性；
- 方药、剂量和加减必须与患者、诊次、病机和来源绑定，不能脱离条件成为通用推荐；
- OCR 纠错值和 OCR 原值同时保留；
- 患者数据进入包前必须去标识化，并记录授权和用途范围；
- 对购买或网上获取的电子资料记录版权、许可和可用范围，不能把“可获得”当成“可用于训练或再发布”。

## 6. 自动蒸馏 Skill 的职责

### 6.1 Skill 的定位

Skill 是自动构建知识包的工作流和质量控制器，不应承载全部医学知识。医学内容主要位于可版本化的数据包中，Skill 负责识别材料、选择抽取策略、调用处理工具、构包、校验和生成报告。

### 6.2 输入材料识别

首版支持 PDF、DOCX、TXT，并识别同一文件中的混合片段：

- 教材/专著：概念、分类、机制、辨析与方法；
- 名医经验：主张、辨证抓手、用药特点、适用条件与禁忌；
- 医案：诊次、四诊信息、病机、治法、处方和转归；
- 论文：研究问题、样本、方法、结果、证据级别与局限；
- 表格和图片：逐项转录、结构恢复、位置和识别质量。

不能只给整份材料贴单一类型标签；应以章节、段落、表格或案例为单位路由到相应抽取器。

### 6.3 自动构建流水线

1. 材料登记、指纹计算、版权与隐私状态记录；
2. 版面解析、OCR、章节恢复、表格与图片识别；
3. 文档片段分类与医案边界识别；
4. 医学实体、属性、否定、条件、时间和主体抽取；
5. 病机原子与病机关系抽取；
6. 医家经验模板和医案诊次结构生成；
7. 三层候选对齐和病机路径生成；
8. 规则校验、模型自检与跨片段一致性检查；
9. 高风险和低置信项目进入人工复核队列；
10. 按统一协议生成知识视图、推理视图、包自描述、质量报告和错误清单，不为单包重复生成说明书；
11. 原子化提交、断点保存、版本发布和索引更新。

每一步都应可单独重跑。下游产物记录上游版本，避免只修改结果而无法追踪生成过程。

### 6.4 蒸馏中的医学专项检查

- 病因、病机、证候、证型、病名不能混用；
- 症状出现不等于某病机必然成立；
- 识别“可见、常见、多见、必见、未见”等强度差异；
- 识别否定、假设、比较、鉴别和引用他人观点；
- 区分医家原按、编者按和系统分析；
- 区分首诊、复诊及处方变化，不能把不同时间点拼成同一状态；
- 识别主次、标本、缓急、兼夹和动态转化；
- 识别同名异义、异名同义以及流派术语；
- 处方反推病机只能作为候选证据，不能替代原文判断；
- 治疗后好转不能单独证明病机推断正确。

### 6.5 Agent 平台分工与闭环

平台可以由一个总控 Agent 调度若干职责明确、可替换的工作单元。这里的“多个 Agent”是工程模块分工，不意味着让多个模型自由讨论后直接形成医学结论。

- 摄取 Agent：登记材料、恢复版面、识别章节与医案边界；
- 证据抽取 Agent：提取实体、关系、条件、否定、时序和来源主体；
- 病机构造 Agent：形成病机原子、复合病机和动态路径候选；
- 三层对齐 Agent：连接基础病机、医家经验与医案实例；
- 质检 Agent：检查证据覆盖、逻辑矛盾、重复、遗漏和格式；
- 医学评审 Agent：依据标注指南提出复核建议，但不能替代专家签署；
- 发布 Agent：依据统一协议执行结构与自描述检查、版本化并更新索引；
- 运行时会诊 Agent：按用户类型和病例上下文检索所需子图，组织问询与回答；
- 安全守门 Agent：独立检查红旗征象、越权诊断、处方风险和隐私泄露。

总控流程采用显式状态机：`待摄取 → 已解析 → 已抽取 → 待对齐 → 待复核 → 可发布/隔离 → 已发布 → 已废止`。每个状态都保存输入版本、输出版本、责任主体和失败原因。模型评分不能自行把高风险医学内容从“待复核”提升为“已确认”。

平台的自动完善来自受控反馈闭环：把专家修改记录成结构化差异，归因到 OCR、实体、关系、对齐、推理或安全错误；只有在独立验证集上改善且不降低安全指标的规则、提示或模型版本，才能进入下一版。不能把所有对话内容未经审核直接回灌为知识。

### 6.6 Skill、知识包与平台代码的边界

- Skill 保存稳定的构建流程、路由标准、质量门和工具使用说明；
- 知识包保存医学内容、证据、映射、版本与最小机器自描述；
- 项目级统一知识包协议说明书保存所有包共同的数据解释、调用规则和兼容边界；
- 平台代码负责文件处理、任务队列、图与索引、检索、权限、审计和界面；
- 模型负责语义抽取、候选生成、自然语言交互，不作为唯一数据库；
- 人工评审负责高风险结论、金标准、争议裁决和发布批准。

这种边界可以避免每增加一本书就复制一个庞大 Skill，也能避免更换模型时整个知识体系失效。

对方剂模块而言，“Skill 保持轻量”不等于拆散整方。一个 FormulaSkill 只保存稳定能力定义和任务接口，医学内容仍来自知识包；统一药物层管理药物单元和整方关系。单味药基础知识可以被多个方剂引用，但不默认成为独立安装的顶层 Skill。

### 6.7 后训练数据与模型闭环

当前阶段只把专家修改和运行错误保存为隔离候选资产，并记录去标识化、许可、错误归因、证据和版本信息，不自动生成正式训练集或启动训练。普通对话、未审核医案和来源不清材料不得直接进入训练候选。

待智能体和蒸馏结构跑通后，未来方案再根据当时的任务接口、数据分布、模型、算力和基线表现，确定训练数据划分、训练方法、具体验收指标、灰度和回滚。方剂组成、剂量、炮制、加减、停调、禁忌和患者自助用药必须继续作为安全关键验收类别。详细规划见 [md/13-方剂药物层与后训练方案.md](md/13-方剂药物层与后训练方案.md)。

## 7. 热插拔与多科扩展

### 7.1 热插拔的准确含义

把兼容包复制到约定目录后，平台应自动：

1. 发现包并读取清单；
2. 依据平台内置的统一说明书校验协议版本、结构、完整性、受控依赖、许可和安全级别；
3. 建立独立命名空间；
4. 增量构建本包索引和跨包候选映射；
5. 只让已通过校验的能力进入查询与推理；
6. 移除包时撤销其索引和映射，不影响其他包；
7. 升级包时保留旧版本和可回滚状态。

热插拔不等于“把文件夹里的所有文本都塞入提示词”，也不等于所有包拥有同等可信度。

### 7.2 防止多科知识冲突

- 每个概念使用稳定标识，不以显示名称作为唯一身份；
- 每条断言属于具体来源和适用范围，不能全局覆盖；
- 专科、医家、流派、年代和人群作为作用域参与检索；
- 通用本体只提供对齐锚点，不强制消除学术分歧；
- 冲突以并列断言和冲突关系表达，由证据和场景决定呈现；
- 路由器先确定任务、科室和风险，再选择知识包；
- 检索结果按来源质量、任务匹配度、证据完整性和审核状态排序，而不是按“新包覆盖旧包”；
- 对跨科共享病机建立共同概念，对专科特义保留子概念和作用域。

### 7.3 防止系统膨胀

- Skill 保持轻量，复杂医学知识按包按需加载；
- 先检索包级元数据，再检索节点，最后展开证据，不一次装入全部内容；
- 使用分层索引和查询预算限制召回数量；
- 会话中只保留当前任务相关的病机子图；
- 历史病例、患者记忆和公共知识分库存储，禁止混写；
- 定期检测重复节点、孤立节点、失效映射和版本漂移；
- 新科室通过兼容性测试后接入，不修改已有包的来源事实。

### 7.4 LadybugDB 与多包联合使用

平台默认采用 LadybugDB 作为嵌入式图数据引擎，以满足少依赖、无独立数据库服务和随平台发行的要求。LadybugDB 负责运行时保存与查询知识节点、关系、病机路径、必要原文证据、包身份、版本和审核状态；大型原始文件可以由平台文件存储管理，并在图中保留可靠定位。

知识包是不同人员独立蒸馏和交换的产物，不直接等同于一个独立数据库。多个同科目数据包放入约定位置后，平台自动发现、校验、暂存并纳入该科目的 LadybugDB 工作库，随后更新全文、向量和图索引以及跨包候选映射。查询可以同时覆盖教材、论文、名医经验和医案，同时保留每条结果的来源包、版本和适用范围。

统一工作库不能吞并来源边界。来源节点继续独立存在，通用病机只提供联合查询入口；数据包停用或卸载后，其知识和映射不再影响默认结果。首次纳入大包和建立索引允许耗时，但过程必须自动、可观察、可恢复，不要求用户安装数据库、Docker 或额外启动数据库服务。

同一工作库由平台保持单一受控写入方。普通智能体只通过平台能力访问，不直接读写数据库文件。不同智能体环境以后按各自支持的 Skill、插件、MCP 或 API 方式逐步适配，不能因此改变知识包语义。

## 8. 运行时医学智能体

### 8.1 智能体由五部分组成

> 医学知识包 + 病机推理策略 + 基础模型 + 上下文/记忆管理 + 医疗安全策略

安全策略必须是独立且优先的层，不能只写在角色提示词里。

### 8.2 角色模拟的正确实现

“以医生身份扮演他的逻辑”应实现为可切换的专家推理配置：关注点、证据权重、辨证顺序、病机组合偏好、鉴别规则、治法映射和表达风格。模型仍可综合当前证据，不能被单条经验机械约束。

运行时应允许：

- 使用某位医家的视角解释病例；
- 并列比较多位医家的可能路径；
- 使用通用病机框架补充结构，但不得冒充某医家原话；
- 当医家资料不足时明确降级为一般分析；
- 展示支持证据、反证、缺失信息和替代解释；
- 通过追问补足对病机区分最有价值的信息。

### 8.3 对话流程

1. 识别用户是患者、学生、研究者还是临床专业人员；
2. 先检查急危重红旗征象和不适合继续对话的情况；
3. 解析已有症状、病史、检查、舌脉和时间变化；
4. 检索适用专科、基础病机和医家包；
5. 生成多个候选病机路径及其支持/反对证据；
6. 选择信息增益最高且数量受限的追问；
7. 更新候选路径，而不是过早锁定单一结论；
8. 面向普通用户提供教育性说明、就医建议和风险提示；
9. 面向获授权的专业人员提供带出处的决策支持，但保留人工确认；
10. 仅保存必要、获同意且去标识化的记忆。

### 8.4 医疗输出边界

“禁止给出具体诊断”并不足够，因为具体用药同样可能造成直接医疗风险。面向公众的默认模式应当：

- 不宣称确诊；
- 不独立开具具体处方、剂量或替代现有治疗；
- 不因中医解释而延误现代医学检查或急救；
- 对急症、孕妇、儿童、老年人、慢病、多药合用和肝肾功能异常提高限制；
- 对可能相互作用、毒性药材和禁忌给出明确警示；
- 输出可能性、依据、缺失信息和建议就医科室；
- 明确资料来源和系统能力边界。

若论文需要研究方药推荐，应限定为“历史医案中的治法/方药检索与医生辅助排序”，在离线数据或医生监督环境中评价，不直接部署为患者自助处方功能。

## 9. 上下文与记忆设计

### 9.1 不建议用固定“20%权重”描述

模型中知识包、提示词和记忆的影响无法稳定换算成百分比。更可执行的做法是设置隔离、优先级和预算：

- 医学事实优先来自当前已启用且可追溯的知识包；
- 当前轮明确提供的信息优先于历史摘要；
- 记忆只能补充背景，不能覆盖本轮证据和安全规则；
- 每轮对可用记忆设置条数或 token 预算；
- 未被当前任务召回的记忆不进入上下文；
- 冲突时展示差异并重新确认，不静默选择旧记录。

### 9.2 记忆分层

- 会话工作记忆：本轮症状、候选病机、已问问题和待确认项；
- 阶段摘要：一次连续讨论的结构化摘要，可随新证据更新；
- 长期偏好：用户允许保存的语言、展示方式和研究偏好；
- 临床资料：单独的高敏感数据域，默认不进入普通长期记忆；
- 系统学习反馈：去标识化、经审核的错误类型和规则改进，不直接记住个体病例。

记忆写入应当可解释、可查看、可修改、可删除，并有保存期限。对患者健康信息必须获得明确授权和满足伦理、隐私及数据安全要求。

## 10. 自动评审与质量评价

### 10.1 四道质量门

1. 结构门：格式、必填项、引用位置、版本和依赖正确；
2. 证据门：每个关键断言可回溯，原文与模型内容不混淆；
3. 医学门：病机结构、主次、时序、鉴别和治法关系合理；
4. 安全门：无越权诊断、危险用药、隐私泄露和版权违规。

未通过的包可进入隔离区供修复，但不能自动进入生产检索。

### 10.2 抽取与构造指标

- 文档覆盖率、OCR 字符错误率和表格恢复准确率；
- 病因、病位、病性、病机、证候、治法等实体的精确率、召回率、F1；
- 因果、支持、鉴别、演变、治疗等关系 F1；
- 主病机、兼夹病机和诊次归属准确率；
- 原文定位准确率和证据充分率；
- 原文陈述与模型推断混淆率；
- 三层节点对齐的准确率、召回率及错误合并率；
- 完整病机路径正确率或图编辑距离；
- 同一材料重复运行的一致性和版本可复现性。

在医学场景中，应特别控制错误合并率。把两个不同病机错当成同一个，通常比暂时不合并更难发现且污染范围更大。

### 10.3 专家评价维度

由至少两名具备相应背景的中医专家独立评价：

- 病机抓取是否准确；
- 主次层级是否合理；
- 证据是否充分且没有断章取义；
- 医家特色是否保留；
- 鉴别路径是否完整；
- 治法与病机是否一致；
- 病程演变是否符合原案；
- 输出是否有临床误导风险；
- 结构是否比原文更便于检索、教学和病例讨论。

报告专家一致性，如 Cohen's kappa、Fleiss' kappa 或 Krippendorff's alpha，并保留分歧，不只报告平均分。

### 10.4 运行时任务评价

构造不同科室、医家、常见证、复杂兼夹证、信息缺失、相似证鉴别和急危重红旗的测试集，评价：

- 候选病机召回率和首位准确率；
- 关键追问的信息增益与冗余率；
- 引用正确率、忠实度和无依据生成率；
- 医家视角一致性及跨医家串味率；
- 新包安装/移除后的行为正确性；
- 冲突知识的识别与并列呈现能力；
- 拒答、转诊和风险提示的正确率；
- 不安全诊断/用药建议率；
- 医生完成病例分析所需时间及可用性评分。

### 10.5 临床验证分阶段进行

1. 回顾性医案重建：只给系统就诊前信息，预测原医家病机与治法，再与原案比较；
2. 专家盲评：隐去系统名称，对人工整理、普通 RAG 和本系统输出随机盲评；
3. 模拟病例研究：评价追问、鉴别和安全性；
4. 医生在环的前瞻性辅助研究：系统只作辅助，由医生决定是否采纳；
5. 若涉及真实患者、疗效或个人健康数据，先取得伦理审批、知情同意和数据治理许可。

回顾性“与名医原案一致”不等于临床有效，论文必须区分知识还原效果、推理支持效果和真实临床结局。

## 11. 论文研究设计建议

### 11.1 可凝练的科学问题

> 如何从异构中医文献中自动抽取病机原子及其证据，构造通用病机—名医经验—个体医案三层可审计推理图，并在保留医家差异和病机动态性的同时实现可靠对齐与可插拔临床决策支持？

### 11.2 建议突出四个创新点

1. 面向病机的细粒度证据化蒸馏，而非普通段落切分和向量检索；
2. 三层树形展示与底层病机图的统一表达；
3. 带适用条件、时序、反证和来源的跨层自动对齐；
4. 面向多医家、多专科热插拔的冲突保留与运行时组合技术。

上下文记忆可作为工程支撑和消融因素，不宜与三层病机结构并列为核心创新，否则论文主线会分散。

### 11.3 基线与消融实验

至少比较：

- 原始长上下文直接问答；
- 普通分块 RAG；
- 只有知识节点、没有病机链；
- 有病机链、没有三层对齐；
- 完整三层对齐系统；
- 去掉专家包、医案包、反证关系或时间关系后的消融版本。

这样可以证明提升究竟来自数据量、检索方式，还是三层结构及对齐技术。

### 11.4 数据集划分与防泄漏

- 按书籍、医家或医案来源划分训练、验证和测试集，不能随机拆段后让同一医案同时出现在两侧；
- 对同一病例的复诊记录整体划分，防止后续诊次泄漏答案；
- 单独设置未见医家、未见病种或未见表述的泛化测试；
- 保留困难负例，如同名异义、相似证候、同方异证和同病异治；
- 锁定测试集后再调规则，记录模型、提示词、词表和数据版本。

## 12. 首版范围与里程碑

### 12.1 建议的最小可行研究范围

首版不要同时铺开所有科室。建议选择：

- 一本基础病因病机教材，构造第一层；
- 一位资料较完整的名医或一个学术流派，构造第二层；
- 与该专家相关、具有复诊与疗效记录的一批医案，构造第三层；
- 一个边界明确的病种群或专科作为验证场景。

在单一闭环上证明自动构造和对齐有效后，再复制到第二位专家或第二个专科验证热插拔和泛化能力。

### 12.2 里程碑

1. 确定病机最小表达单元、关系集和标注指南；
2. 完成《病因病机学说》的基础病机包和人工金标准样本；
3. 完成一个名医经验包与一个医案包；
4. 实现三层候选对齐、人工审核和版本追踪；
5. 实现包发现、安装、卸载、冲突检测和联合检索；
6. 实现基于病机子图的病例讨论智能体；
7. 完成自动指标、专家盲评、基线和消融实验；
8. 在伦理和权限满足时开展医生在环验证。

## 13. 主要风险与控制

### 13.1 概念风险

- 病机、证候和证型边界在不同文献中并不总一致：通过来源断言与规范概念分离解决；
- 三层并非天然严格树结构：底层采用图，按任务投影为树；
- 医家经验可能来自后人总结：保存陈述主体和引证层级；
- 从处方反推病机存在多解：只作为候选，不作为来源事实。

### 13.2 数据风险

- OCR 错误会污染术语、剂量和否定词：重点字段双重校验并保留页面定位；
- 数据量增加不必然提高效果：优先提高证据质量、覆盖多样性和标注一致性；
- 网络资料质量不一：建立来源等级、版权状态和纳入/排除标准；
- 购买电子材料不当然取得训练、改编或再分发权：购买前确认许可。

### 13.3 评价风险

- 只与原医案答案比较会鼓励记忆：使用来源隔离和未见样本；
- 专家之间本来就可能不同：报告一致性和分歧原因；
- 自动指标高不代表临床安全：单独评价红旗、禁忌和不安全输出；
- “临床验证”措辞过强：没有前瞻性真实世界设计时，应称回顾性验证、专家评审或模拟验证。

### 13.4 产品风险

- 角色扮演可能让用户误认为真实医生：界面持续标识 AI 辅助身份；
- 长期记忆可能积累错误和敏感信息：默认少存、按需召回、可删除；
- 热插拔包可能带来提示注入或恶意规则：内容与执行指令隔离，包不能任意执行代码；
- 包更新导致结果漂移：版本固定、回归测试和一键回滚。

## 14. 当前需尽快确定的问题

以下问题不会阻止原型启动，但会影响论文范围和标注方案：

1. 首个验证专科或病种群是什么；
2. 首位名医是谁，是否有成体系的经验材料和足量医案；
3. “三层”的第二层按单个医家、学派还是某部经验著作组织；
4. 病机金标准由谁标注，能否安排两名以上专家独立复核；
5. 首版是否只做病机与治法，还是纳入具体方药和剂量；
6. 目标用户优先是中医学生、研究人员还是临床医生；
7. 可用材料的版权、患者授权和伦理条件；
8. 论文计划采用回顾性医案、模拟病例还是医生在环研究。

其中第 5 项的架构已确定为组合式 FormulaSkill 与统一药物层；仍待确定的是首版纳入多少方剂、哪些剂量字段可以进入研究数据，以及高风险任务开放到何种用户范围。后训练基础模型、训练方法、可用算力、训练许可、验收阈值和模型权重发布范围不在当前阶段确定，待核心系统跑通后统一纳入实施方案。

## 15. 最终验收基线

首版达到以下结果，才可认为完成了导师所述方向，而不只是做出普通知识库：

- 能从教材中自动形成有来源的基础病机分类与关系；
- 能从名医材料中抽取其特有病机观点、判断依据、适用条件和治法映射；
- 能把医案按诊次重建为动态病机链；
- 能将三层自动对齐，并正确保留不确定、冲突和无法对齐的内容；
- 任一关键病机节点或路径可回到原文和具体位置；
- 能以树查看层级、以链查看推演、以图查看兼夹和演变；
- 新专家包或专科包加入后无需改动核心智能体即可使用；
- 移除或升级数据包不会污染其他来源，且可复现旧结果；
- 相比普通 RAG，在病机抽取、路径还原、专家风格隔离和病例分析上有显著、可重复的提升；
- 通过医学专家盲评和安全测试，且不会对公众越权作出诊断或具体处方。
- 一个方剂能够以单一组合式 FormulaSkill 运行，药物层统一管理多味药及其配伍关系，而不是依赖多个孤立的单味药顶层 Skill；
- 核心系统能够保存未来后训练所需的证据血缘、专家修订、错误归因和可复现基线；实际后训练启动后，再按届时批准的方案验收效果、证据忠实度和安全关键指标。

## 16. 一句话产品定义

> 一个以病机为主线，把教材理论、名医经验与个体医案自动蒸馏并对齐为三层可审计病机图谱，支持多专家、多专科知识包热插拔和医生在环验证的中医智能体平台。
