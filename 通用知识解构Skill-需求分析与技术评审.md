# 通用知识蒸馏能力：需求评审基线

> 文档状态：早期需求评审稿  
> 当前目标：确认产品目标、输入输出、行为边界和验收方向  
> 本阶段不讨论：具体存储技术、字段 Schema、程序架构、文件命名或 Skill 实现

> 说明：本文保留为通用知识蒸馏的历史需求基线。当前中医病机专项方案、LadybugDB 选型和多包联合使用要求，以 [md/README.md](md/README.md) 及其分项文档为准。

## 1. 产品定义

本产品用于把书籍、案例集、论文等源材料蒸馏成可独立使用的知识数据包。

用户提供源文件后，系统应：

1. 识别文件载体和材料的知识类型；
2. 选择与材料类型相符的知识解构方式；
3. 提取材料中有依据的知识、原文和关系；
4. 支持全量处理或指定范围处理；
5. 支持中断、保存进度和继续处理；
6. 记录无法处理的页面、图片和其他错误；
7. 按项目级统一知识包说明生成数据包及其最小机器自描述；
8. 让多个兼容数据包能够被统一发现、联合查询和关联使用。

产品终点不是提出蒸馏方案，而是生成可供人、AI、Skill、智能体、程序或脚本实际调用的知识数据包。

## 2. 正式产物

一次完整蒸馏的正式产物是：

> **符合统一知识包协议的数据包 + 最小机器自描述**

知识数据包至少在逻辑上包含：

- 源材料身份和版本信息；
- 蒸馏得到的知识节点；
- 节点之间的关系；
- 与节点或关系对应的原文数据；
- AI 生成的摘要、标签和其他分析结果；
- 原文与分析结果之间的对应关系；
- 处理范围和完成状态；
- OCR、抽取、图片识别及其他错误记录；
- 人工或 AI 修改记录；
- 数据包自身的类型、能力、兼容版本和限制自描述。

数据包不要求携带原始 PDF、Word 文件、页面图片或裁剪图片，以控制包体积。它只保存必要的文本数据和源位置。

对于扫描材料，数据包中的“原文”实际是未经语义改写的 OCR 原始识别文本，而不是原始页面本身。必须保留页码、区域或其他来源定位，并明确其可能存在 OCR 错误。用户如需对照视觉原件，需要另外持有源文件。

## 3. 知识保真边界

### 3.1 允许保存的内容

- 源材料明确表达的事实、定义、观点、主张、规则、案例、步骤、结论和关系；
- 源材料自身给出的推论、解释或判断，但必须标明其来源主体；
- 原文摘录或 OCR 原始文本；
- AI 对原文生成的不改变原意的摘要；
- AI 为组织和检索生成的节点名称、类型标签、主题标签和规范化结果；
- 节点与关系的置信状态、复核状态和错误状态；
- 用户或 AI 对错误内容作出的修正及其记录。

### 3.2 禁止混入的内容

- 源材料没有表达的新领域知识；
- 仅凭常识补出的结论；
- 把个案自动升级成一般规律；
- 把相关性自动解释成因果关系；
- 把模型猜测伪装成作者观点或原文事实；
- 没有证据定位、却未明确标记来源缺失的知识内容。

### 3.3 原文与 AI 分析必须并存

AI 摘要、节点命名和类型判断可以进入数据包，但必须与原文分开保存并建立对应关系。

调用者应能够：

- 只读取原文；
- 只读取结构化知识或摘要；
- 同时查看分析结果及其原文依据；
- 判断某项内容来自原文、AI 分析还是后续人工修改。

AI 分析不能覆盖或替换原文。

## 4. 输入范围

### 4.1 首版文件载体

首版需要支持：

- PDF；
- DOCX；
- TXT。

PDF 同时包括：

- 具有可用文本层的 PDF；
- 扫描 PDF；
- 包含普通插图、知识图、表格或其他图片内容的 PDF。

图片内容无法识别时，必须记录错误而不是阻断整份材料。

### 4.2 材料知识类型

文件载体和知识类型是两个不同维度。PDF、DOCX 或 TXT 只是容器；同一种容器可以承载不同知识类型。

首批必须识别和处理：

1. **知识点型材料**：概念、分类、原理、机制、属性、标准和方法；
2. **案例型材料**：背景、现象、判断、处理、结果和复盘；
3. **论文型材料**：研究问题、方法、样本、结果、结论、证据和局限。

系统必须允许混合型材料。同一份书或文件的不同章节可以使用不同解构方式，不能只给整份材料贴一个标签后强制套用同一模板。

### 4.3 后续可能扩展的材料类型

- 指南、规范和标准；
- 操作手册和流程文件；
- 题库与问答集；
- 综述；
- 专家访谈和讲稿；
- 网页；
- 音视频转写材料。

这些类型当前只作为扩展方向，不纳入首版承诺。

## 5. 知识组织要求

### 5.1 节点

知识以节点和关系进行组织。节点是能够被独立查找、引用和关联的知识单位。

节点粒度不应固定为一句话，也不能一律使用整段或整章。系统应根据材料类型确定合理粒度，同时满足：

- 节点可独立理解；
- 不丢失成立条件和重要上下文；
- 可返回上级章节、案例、论文或其他所属结构；
- 便于查询和跨包关联；
- 不因过细拆分产生大量失去语义的碎片。

### 5.2 关系

关系需要支持分层组织。需求上至少区分：

- 文档结构关系；
- 材料明确表达的知识关系；
- 术语与表达规范化关系；
- 跨包同一概念或近似概念关系；
- 通用关系及其更具体的子关系。

对于“导致”“会引起”“可造成”等文字不同但语义相同或相近的关系，不能仅因汉字或介词不同就创建互不相干的关系类型。

同时必须保存材料使用的原始关系表述。规范关系负责统一查询，原始表述负责保真和复核。无法可靠归一时，应保留原始关系并标记为待确认，不能强行归类。

### 5.3 关系体系的范围

关系体系建议采用两层需求模型：

- 少量跨领域通用关系，用于文档结构、来源、引用、等同、近似、上下位和一般关联；
- 材料类型或领域专用关系，用于表达医学、法律、工程等领域中的具体含义。

这样可以避免所有关系都各自为政，也避免用少量过度宽泛的关系损失专业语义。具体关系名称和层级留到后续设计。

## 6. 跨包关联与热插拔

### 6.1 热插拔的含义

多个兼容数据包放入约定位置后，统一调用方应能：

- 自动发现数据包；
- 读取包的身份、类型、版本和能力；
- 检查兼容性和完整性；
- 查询单个包或多个包；
- 自动建立可用的跨包映射；
- 加入新包后更新联合查询结果；
- 移除某个包后撤销该包提供的内容和映射；
- 不要求为每个新包重新理解一套调用方法。

热插拔不意味着把所有包永久合成一个包，也不意味着同一节点只保留一份数据。

### 6.2 来源节点与联合概念

跨包关联时必须区分：

- **来源节点**：属于某一个具体数据包，保存该材料自己的内容、表述、版本和原文；
- **联合概念**：为多个来源节点提供共同查询入口；
- **映射关系**：说明某来源节点为什么以及以何种状态对应某联合概念。

即使多个来源节点被判断为同一概念，各个来源节点的数据也必须全部保留。联合概念只负责汇聚，不能覆盖、吞并或替换来源内容。

### 6.3 同一概念判断

节点名称不能单独决定两个节点是否相同。

判断时至少应考虑：

- 所属领域和上下文；
- 知识类型；
- 节点定义、属性和邻接关系；
- 上下位概念或所属体系；
- 语言、简繁体、别名和术语变体；
- 材料版本、年代和适用范围；
- 已有规范词或权威标识。

判断条件也不能过于严苛，以免真正相同的概念因为文字差异被拆成多个孤立节点。

系统应能够表达不同状态，例如：

- 已确认相同；
- 自动规则判定相同；
- 高可信候选相同；
- 已确认不同；
- 尚未判断。

### 6.4 自动映射和用户规则

只要符合统一的数据要求，跨包映射应能够自动运行并自动生效，不要求用户逐条确认。

为保证健壮性，自动映射必须：

- 记录使用的判断规则和依据；
- 允许用户查看、修正和撤销；
- 不修改或删除来源节点；
- 发生错误时能够重新计算；
- 面对冲突规则时给出明确状态；
- 避免一次错误映射扩散并污染原始包。

产品应自带一套通用联合规则，同时允许用户增加或替换自己的规则。用户规则产生的映射同样需要留痕和可撤销。

## 7. 来源与版本要求

每个知识包必须包含以下来源信息项：

- 书名或材料标题；
- 作者；
- 版次；
- 出版年；
- 出版社；
- ISBN；
- 源文件身份信息；
- 实际处理范围；
- 蒸馏时间和数据包版本。

同一材料的不同版次、不同出版年份或不同文件版本必须能够并存。联合查询不能因为标题和作者相同就覆盖旧版内容。

若不同版本的知识存在差异，系统需要：

- 保留每个版本的原始陈述；
- 允许按版本查询；
- 可以发现差异，但不能擅自判定哪个版本正确；
- 展示差异时标明各自来源和时间。

对于源材料本身没有提供的书目信息，应保留该信息项并明确标记为未知或未提供，不能猜测填写。

## 8. 数据包可修改性

数据包不是发布后永久只读的文件。用户或后续 AI 可以修正 OCR 错误、节点内容、分类、关系、来源信息和其他问题。

修改必须满足：

- 不能静默覆盖原文或旧值；
- 保留修改前内容；
- 记录修改后内容；
- 记录修改对象；
- 记录修改原因；
- 记录修改时间；
- 区分人工修改、AI 修改和重新蒸馏；
- 修改后能够重新校验；
- 必要时能够回退；
- 修改可能影响跨包映射时，应能触发映射更新。

“允许修改”不等于所有调用者默认拥有修改权限。具体权限和操作方式后续再设计。

## 9. 全量、部分处理与断点续做

### 9.1 用户可选择的处理范围

首版至少支持：

- 全部文件；
- 指定文件；
- 指定页码范围；
- 指定章节；
- 从已有断点继续。

“处理指定数量的知识点”不列为首版范围，因为知识数量通常只有完成解构后才能确定。

### 9.2 断点要求

面对数百页材料或长时间任务，系统必须：

- 记录已完成、失败、跳过和未处理范围；
- 中断后保留已经成功写入的数据；
- 能从稳定断点继续，而不必从头开始；
- 重复运行时避免无控制地产生重复节点；
- 不让未完成批次破坏已完成数据；
- 确认源文件版本没有变化后才能续接；
- 能够让用户查看当前进度和剩余范围。

## 10. 错误记录与流程连续性

单个页面、图片、表格、章节或处理步骤失败时，应尽可能继续其他不受影响的内容。

错误记录至少需要表达：

- 出错材料；
- 准确位置；
- 失败对象类型；
- 失败原因或错误信息；
- 已尝试的处理方式；
- 是否重试及重试结果；
- 对最终知识完整性的影响；
- 是否需要用户后续处理。

对于因内容安全限制、图片暴露或血腥内容、格式损坏、清晰度不足等原因无法识别的图片，不能静默跳过，也不能把它标记为“无知识”。

即使错误无法立即解决，数据包也应保持可用，并明确说明其覆盖缺口。

## 11. 统一协议说明要求

统一协议说明的目标是让任何兼容平台、AI 或程序无需阅读单包文档即可正确理解新数据包。

整个项目只维护一套统一知识包协议说明书。材料类型差异作为该说明书中的资料类型配置处理，不为类型或单包另写说明书。

每个包只携带机器可读的最小自描述，说明材料身份、协议版本、资料类型配置、版本、覆盖范围、能力、质量、错误、许可和限制。人类、AI 和程序都按照项目级统一说明书解释这些值，单包不得自定义另一套调用语义。

不同类型包共享同一核心知识结构，可以启用不同的可选内容，但必须共享统一的发现、识别、基础查询和兼容逻辑。

## 12. 数据包粒度

首版采用：

> **一份原始源材料对应一个知识数据包。**

多个文件只有在它们共同组成同一份原始材料时，才可以作为一个包处理，例如一本书被拆成多个文件。

多份独立材料共同生成一个包暂不纳入首版。未来可以扩展，但不能破坏首版“一份来源一个包”的来源边界。

## 13. 已确认的需求

- 最终产物是符合项目级统一协议的知识数据包和最小机器自描述；统一说明书属于项目资产，不随单包重复生成；
- 数据包保存知识本身、AI 分析结果和原文数据，但不携带大型原始文件或图片；
- AI 可以生成摘要、节点名称和类型标签，但不能生成推测新的领域知识；
- 原文与 AI 分析必须分开保存并可对照；
- 数据包支持后续修改、留痕、回退和重新校验；
- 跨包映射通过统一数据要求自动运行和自动生效；
- 产品自带联合规则，并允许用户自定义联合规则；
- 首版支持 PDF、DOCX 和 TXT；
- 扫描 PDF 的 OCR 和图片内容处理属于首版范围；
- 首版支持全量、文件、页码、章节和断点范围，不支持预先指定知识数量；
- 书名、作者、版次、出版年、出版社和 ISBN 是必要来源信息项；
- 关系采用通用关系与领域或材料专用关系相结合的分层模型；
- 全项目使用一套统一知识包说明书，材料类型作为其中的配置，单包只提供机器可读的差异信息；
- 首版默认一份源材料生成一个包；
- 热插拔和跨包联合查询属于核心使用需求，而不是远期附加能力。

## 14. 需求验收方向

- 能区分文件载体和知识材料类型；
- 对知识点、案例和论文采用不同解构方式；
- 每个知识节点都能返回来源位置和原文数据；
- AI 摘要和原文可同时查看且不会互相覆盖；
- 扫描材料明确区分 OCR 文本与视觉原件；
- 不把模型推测写成来源知识；
- 同名异义节点不会仅凭名称被合并；
- 简繁体、别名和真正同义表达能够被合理关联；
- 自动跨包映射有依据、可修改、可撤销和可重建；
- 联合查询保留所有来源节点及其版本；
- 不同材料版本可以并存和分别查询；
- 不同文字表达的同类关系可以被统一查询，同时保留原始表述；
- 指定范围完成后可以继续其他范围；
- 任务中断后已完成数据仍然有效；
- 失败页面和图片具有准确错误记录；
- 新包加入后不需要重新学习整套使用方法；
- 修改后的包保留历史并能重新校验；
- 数据包缺少某段内容时能够明确暴露覆盖缺口。

## 15. 尚待确认的问题

> 现行状态说明：本文是历史需求评审稿。15.1、15.2 和 15.4 已在 2026-09-24 的现行设计中解决，以 `md/04-知识包与证据数据模型.md` 和阶段 01 技术设计为准；15.3 与 15.5 留到对应功能进入实施时确认。

### 15.1 必要来源信息缺失时的处理

已经确认书名、作者、版次、出版年、出版社和 ISBN 都是必要信息项。但现实中论文、内部材料、古籍或部分电子资料可能天然没有其中某些值。

现行决议：这些信息项的 key 必须存在；材料未提供时值为 JSON `null`，不能猜测。缺失不阻止实验包生成，正式发布由后续质量门判断。

### 15.2 AI 摘要允许的改写程度

现行决议：允许把同一明确范围内的多段内容整理成一条结构化知识，但每项断言必须标注对应证据；跨范围或连接语义不明确时只能作为待复核候选。

### 15.3 自动映射的错误容忍

已经确认映射可以自动生效。仍需确定：低置信或规则冲突的映射是否也自动进入联合查询，还是仅高可信映射自动生效，其余保持候选状态。

### 15.4 修改后的“原文”边界

现行决议：OCR 或转录原值不可覆盖，纠正文本和确认状态分开保存；确认后的纠正文本可以作为默认可读展示，但必须能够立即查看原值。

### 15.5 图片知识的最低交付

图片无法识别时已经确定要记录错误。还需要确认：图片识别成功时，数据包保存图片内容的文字描述和结构化知识是否足够，还是还要保存图片中文字的逐项转录及其位置。

## 16. 下一轮评审焦点

下一轮优先确认第 15.1 项。它会影响论文、古籍和非正式资料能否进入系统，也决定“正式包”和“信息不完整但仍可用的包”是否需要区分。随后再确认 AI 摘要边界和自动映射的错误容忍。
