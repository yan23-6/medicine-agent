# 项目执行状态

> 状态更新时间：2026-09-24  
> 当前阶段：01 Skill 基础与最小生产消费闭环  
> 当前任务：T01 可调用 Skill 基础与最小知识闭环  
> 当前任务状态：in_progress

## 1. 已存在

- 中西医平台需求与技术设计文档；
- 通用医学主干、中医与西医分支设计；
- Agent、Skill、知识包和病例数据模型的逻辑边界；
- LadybugDB、MCP、REST API、医疗安全和后训练方向；
- 导师参考材料；
- 项目任务系统和阶段需求草案。
- T01 技术设计与 Codex/MCP 适配契约；
- uv 管理的 CPython 3.13 开发环境、`pyproject.toml` 与依赖锁文件；
- 第一版 Skill Registry、Runner、中立 Facade、统一 DTO 和错误信封；
- 六个最小 Skill、FakeModel、OpenAI-compatible 模型适配器和 CLI；
- 可生成、校验、查询并回溯证据的目录型实验包；
- 第一批单元、契约和端到端测试。

## 2. 尚不存在

- 完整或可发布的核心平台；
- 已实现的 Agent；
- 完整中医、西医、病例、方剂或安全 Skill；
- 冻结的正式知识包 v0 Schema；
- LadybugDB 导入与查询实现；
- 正式金标准、正式知识包和自动蒸馏结果；
- Web、MCP 或 REST 服务；
- 医学评测基线和后训练资产；
- 已通过用户真实 API 的在线 LLM 冒烟验收；
- PDF、DOCX、OCR、批处理、断点恢复和正式发布服务。

## 3. 当前禁止假定

- 不得假定 `SKILL.md` 即等于可调用 Skill；
- 不得假定连接 LLM 后自动具备医学抽取、病例标准化或知识包能力；
- 不得要求使用尚未实现的 Skill 蒸馏材料；
- 不得开始批量或正式知识包蒸馏；
- 不得把设计文档中的 Agent 角色写成已运行组件；
- 不得直接把导师资料改写成正式知识或训练数据。

## 4. 当前进入条件

T01 已进入实现。当前机器已经安装 uv 0.12.18 和 uv 管理的 CPython 3.13.15，项目骨架与离线最小闭环已经运行。下一步是补齐 Schema 导出、运行记录、字段歧义处理实现、真实 LLM 冒烟测试和干净环境验收，不能因骨架可运行就提前进入 T02。

## 5. 状态更新规则

T01 只有在 Skill 可被真实调用、最小生产消费闭环通过自动检查、所有产物可从干净环境重建后才可标记为 `done`。完成后把 T02 提升为当前任务。

